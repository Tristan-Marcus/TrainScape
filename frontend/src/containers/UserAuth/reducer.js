import { ActionTypes } from "./constants"

const defaultState = {
    
}

export default function userAuthReducer(state = defaultState, action) {
    switch(action.type) {
        default:
            return state
    }
}